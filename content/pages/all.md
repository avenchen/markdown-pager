# 歡迎使用 Markdown Pager

這是一個常駐最上層的小工具，用於快速檢視多個 Markdown 文案。

- 使用頂部按鈕在頁面間切換
- 可透過「選擇資料夾」切換不同文案目錄
- 支援多份 `.md` 檔案，依檔名排序

# 操作提示

1. 透過「首頁」返回第一個 Markdown 檔案。
2. 「上一頁」「下一頁」可循環瀏覽所有檔案。
3. 若資料夾內容更新，可再次使用「選擇資料夾」載入。

> 小提醒：此工具顯示原始 Markdown 文字，保持文案可讀性。