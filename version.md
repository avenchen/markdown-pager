# 版本紀錄

## 1.2 - 2025-11-17
- 新增方向鍵快捷鍵支援：
  - 上一頁：上方向鍵（↑）
  - 下一頁：下方向鍵（↓）
  - 首頁：左方向鍵（←）
  - 最後：右方向鍵（→）
- 方向鍵快捷鍵為固定功能，不依賴設定檔

## 1.1 - 2025-11-17
- 新增打包功能，可使用 PyInstaller 打包為 Windows 可執行檔
- 提供 build.bat 和 build.ps1 打包腳本
- 打包後的執行檔可在其他 Windows 電腦上獨立執行，無需安裝 Python

## 1.0 - 2025-11-17
- 按鈕文字改回完整描述（首頁/上一頁/下一頁/最後/設定/關閉等）
- 設定視窗內的「套用」「儲存快捷鍵」「選擇 Markdown 檔」「確定」「取消」等按鈕恢復全名
- 維持縮小高度與寬度的同時，保留可讀性

## 0.9 - 2025-11-17
- 設定視窗加大至 520x680，確保底部按鈕可見
- 工具列與設定內所有按鈕改為單字並縮小高度/寬度
- 關閉按鈕回復主題色（移除紅底）

## 0.8 - 2025-11-17
- 設定視窗高度調整並新增快捷鍵設定（Ctrl + 字母）
- 工具列按鈕文字縮短、尺寸減半，新增「末」按鈕
- 字級調整移至設定視窗（滑桿）並支援 Ctrl +/- 快捷鍵
- 快捷鍵儲存於 `config.json`，啟動時自動套用
- 設定頁顯示目前檔案與快捷鍵提示

## 0.7 - 2025-11-17
- 新增「最後」按鈕與章節跳至末尾功能
- 工具列高度縮減、按鈕 padding 調整並新增字級提示
- 設定視窗加入顯示區字級滑桿
- 內容區支援 Shift + H/P/N/L 快捷鍵與 Ctrl +/- 字級調整
- 捲軸改為淡藍色樣式

## 0.6 - 2025-11-17
- 「選擇資料夾」改為於設定視窗內選擇單一 Markdown 檔
- 章節換頁改採 Markdown 中的 `# ` 標題分段
- 新增章節狀態列顯示檔名與進度
- 新增淡藍色捲軸樣式與 Text-based rich 渲染流程
- 設定檔保存/載入紀錄最後開啟的 Markdown 檔

## 0.5 - 2025-11-17
- 導入 modern 深色 UI / UX：統一色票、扁平化按鈕、hover 與重點色
- 重新設計工具列與內容區，加入邊框與圓角，優化閱讀體驗
- Markdown 顯示（HTML/文字模式）套用新主題配色與排版
- 設定視窗同步使用深色主題，滑桿／Spinbox 樣式更新
- 新增共用按鈕 helper，確保互動一致

## 0.4 - 2025-11-17
- 新增「設定」按鈕，將透明度與視窗大小設定移至彈出視窗
- 實作整個視窗可拖動功能（點擊視窗任意位置，排除按鈕與控制項）
- 實作設定保存與載入功能（`config.json`）
- 保存項目：視窗大小、位置、透明度、字型大小
- 下次開啟時自動載入保存的設定

## 0.3 - 2025-11-17
- 移除自訂標題列，將所有按鈕集中於工具列並新增「關閉」按鈕
- 工具列支援拖動視窗，維持無標題列的簡潔外觀
- 新增透明度滑桿（0.5-1.0），可即時調整視窗透明度
- 新增寬度/高度 Spinbox 與「套用尺寸」按鈕，可設定整體程式大小
- 保持 always-on-top、rich Markdown 顯示與字型縮放功能

## 0.2 - 2025-11-17
- 實作 Markdown rich 顯示效果（支援標題、粗體、斜體、程式碼區塊）
- 添加字型放大縮小功能（+ / − 按鈕，範圍 8-24px）
- 移除標題列，改為自訂標題列與關閉鈕
- 實作視窗拖動功能（透過標題列）
- 添加視窗透明效果（整體透明度 85%，顯示區背景透明）
- 允許視窗調整大小
- 優化 UI 佈局，減少畫面佔用

## 0.1 - 2025-11-17
- 建立 Markdown Pager always-on-top 視窗
- 加入頁面切換與資料夾載入功能
- 撰寫範例 Markdown 內容與專案文件

