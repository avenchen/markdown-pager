# Markdown Pager 規格

## 目的
提供一個常駐最上層的小型視窗工具，能快速瀏覽多份 Markdown 文案並支援頁面切換。

## 功能需求
- 視窗預設 `420x360`、可調整大小並保持 `always on top`。
- 採用 modern 深色 UI（統一色票、扁平化按鈕、漸層重點色）。
- 移除系統標題列，工具列集中所有操作按鈕（含設定與關閉鈕）。
- 工具列提供「首頁」「上一頁」「下一頁」「最後」「設定」「關閉」按鈕（維持精簡外觀但完整文字），支援循環切換；設定視窗內按鈕亦使用完整文字（如「套用」「儲存快捷鍵」「選擇 Markdown 檔」「確定」「取消」）。
- 提供顯示區字級調整（僅設定視窗：滑桿 8-24px；另支援 Ctrl +/- 快捷鍵）。
- 點擊視窗任意位置可拖動視窗（排除按鈕與控制項）。
- 透過「設定」按鈕開啟設定視窗（高度 640px），包含：
  - 透明度滑桿（0.3-1.0），即時調整並顯示數值
  - 視窗大小設定（寬度 200-1600、高度 200-1200），可即時套用
  - Markdown 檔案選擇（`askopenfilename`），選擇後立即載入
  - 顯示區字級滑桿
  - 快捷鍵設定（每個功能對應控制字母，使用 Ctrl + 字母）
- 上下頁按鈕依據目前 Markdown 檔內的 `# ` 章節換頁。
- 顯示目前檔案名稱與章節進度（例：`file.md｜章節 2/5 - Title`）。
- 設定會自動保存至 `config.json`，下次開啟時自動載入：
  - 視窗大小與位置
  - 透明度
  - 字型大小
  - 上次開啟的 Markdown 檔路徑
- Markdown 內容支援 rich 顯示效果並套用新配色；捲軸採淡藍色系。
- 若尚未選擇檔案或檔案解析失敗，需顯示提示訊息。

## 技術細節
- 使用 `tkinter` 建構 UI，透過 `overrideredirect(True)` 隱藏系統標題列。
- 自訂色票 `PALETTE` 與共用字型，透過 helper 建立一致的按鈕與 hover 效果。
- 內容區改用 `Text + Scrollbar`，自訂淡藍色捲軸、深色背景與插入點。
- 整個視窗綁定 `<Button-1>` 與 `<B1-Motion>` 事件實現拖動，排除按鈕與控制項。
- 設定視窗使用 `Toplevel` 搭配 `grab_set()`，並沿用深色主題風格，提供透明度、尺寸、字級、檔案選擇與快捷鍵設定。
- 快捷鍵支援：
  - 方向鍵：上一頁（↑）、下一頁（↓）、首頁（←）、最後（→）
  - Ctrl + 字母：可在設定視窗中自訂（預設 H/P/N/L）
  - Ctrl +/−：調整字級大小
- 透明度以 `attributes("-alpha", value)` 動態控制。
- 視窗尺寸透過 `Spinbox` 取得輸入並套用 `geometry`。
- Markdown 解析改為以 `# ` 章節切段，使用 `Text` 的 tag 呈現標題/粗體/斜體/程式碼。
- 設定保存使用 `json` 模組，儲存至 `config.json`，啟動時自動載入，關閉時自動保存。
- 預設從 `content/pages` 取得第一個 `.md` 作為初始檔案，或載入上次記錄。
- 檔案一律以 UTF-8 讀取，錯誤時顯示訊息。

## 範例內容
- `content/pages/home.md`
- `content/pages/guide.md`

